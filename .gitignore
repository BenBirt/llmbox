# Rust
/target/

# Bazel
/bazel-*
/.bazel/

# Bazel: auto-generated local overrides (not for sharing)
# .bazelrc.local is generated by .envrc and contains session-specific proxy
# credentials — never commit it.
/.bazelrc.local

# Bazel disk cache (personal, not shared)
/.cache/

# Editor / OS
.DS_Store
*.swp
*.swo
