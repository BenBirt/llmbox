# Rust
/target/

# Bazel
/bazel-*
/.bazel/

# Bazel local cache dirs and offline-mirror workaround files (not committed)
/.bcr/
/.repo-cache/
/.distdir/
/.crate-cache/
/.bazel-downloader.cfg

# Bazel disk cache (personal, not shared)
/.cache/

# Local overrides (not for sharing)
/.envrc.local
/.bazelrc.local

# Editor / OS
.DS_Store
*.swp
*.swo
